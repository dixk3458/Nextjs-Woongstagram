import { IoBookmark } from 'react-icons/io5';

type Props = {
  className?: string;
};

export default function BookmarkFillIcon({ className }: Props) {
  return <IoBookmark className={'w-6 h-6' || className} />;
}
