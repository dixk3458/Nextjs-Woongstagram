import { IoBookmarkOutline } from 'react-icons/io5';

export default function BookmarkIconn({ className }: { className?: string }) {
  return <IoBookmarkOutline className={className || 'w-6 h-6'} />;
}
